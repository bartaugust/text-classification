defaults:
  - paths
  - logging
  - data_loading
  - trainer: trainer
  - dataset: hatespeech
  - experiments: minilm
#  - model: minilm
#  - tokenizer: minilm-tokenizer

  - _self_

epoch: 0
data_loading.batch_size:
    128
hydra:
  mode:
    MULTIRUN
  sweeper:
    params:
      dataset: welfake, imdb
      experiments: glob(*)
      epoch: 0,1,2,3,4,5

version:
  1.2.0