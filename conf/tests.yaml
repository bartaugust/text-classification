defaults:
  - paths
  - logging
  - data_loading
  - trainer: trainer
  - dataset: hatespeech
  - experiments: bert
#  - model: minilm
#  - tokenizer: minilm-tokenizer

  - _self_

epoch: 5
data_loading.batch_size:
    128
hydra:
  mode:
    RUN
  sweeper:
    params:
#      dataset: glob(*,exclude=[yahoo,imdb])
#      experiments: glob(*)
      epoch: 0,1,2,3,4,5

version:
  1.2.0