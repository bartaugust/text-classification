defaults:
  - paths
  - logging
  - data_loading
  - trainer: trainer
  - dataset: imdb
  - experiments: minilm
#  - model: minilm
#  - tokenizer: minilm-tokenizer

  - _self_

epoch: 1
data_loading.batch_size:
    128
hydra:
  mode:
    MULTIRUN
  sweeper:
    params:
#      dataset: hatespeech
      experiments: glob(*)
      epoch: 0,1,2,3,4,5

version:
  2.0.0