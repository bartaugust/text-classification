defaults:
  - paths
  - logging
  - data_loading
  - dataset: imdb
  - model: bert
  - tokenizer: bert-tokenizer
  - _self_

hydra:
  mode:
    MULTIRUN
  sweeper:
    params:
      dataset: imdb
#      glob(*)
      model: glob(*)
      tokenizer: glob(*)