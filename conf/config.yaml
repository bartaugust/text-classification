defaults:
  - paths
  - logging
  - data_loading
  - trainer: trainer
  - dataset: imdb
  - model: distilbert
  - tokenizer: distil-bert-tokenizer
  - _self_

hydra:
  mode:
    RUN
  sweeper:
    params:
      dataset: glob(*)
      model: glob(*)
      tokenizer: glob(*)

version:
  1.1.0