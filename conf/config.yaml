defaults:
  - paths
  - logging
  - data_loading
  - trainer: trainer
  - dataset: imdb
  - experiments: minilm
#  - model: minilm
#  - tokenizer: minilm-tokenizer
  - _self_

hydra:
  mode:
    MULTIRUN
  sweeper:
    params:
#      dataset: glob(*,exclude=[yahoo,hatespeech])
      experiments: glob(*)

version:
  2.0.0