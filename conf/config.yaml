defaults:
  - paths
  - logging
  - data_loading
  - trainer: trainer
  - dataset: welfake
  - experiments: bert
#  - model: minilm
#  - tokenizer: minilm-tokenizer
  - _self_

hydra:
  mode:
    RUN
  sweeper:
    params:
#      dataset: glob(*)
      experiments: glob(*)

version:
  1.1.0