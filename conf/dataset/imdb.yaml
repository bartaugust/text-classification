# @package dataset
name:
  imdb

load:
  train:
    _target_: torchtext.datasets.IMDB
    root: ${paths.datasets}
    split: train
  test:
    _target_: torchtext.datasets.IMDB
    root: ${paths.datasets}
    split: test


val_split: 0.8

classes:
  2

